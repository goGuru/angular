import { BackwardsDirective } from './backwards.directive';

describe('BackwardsDirective', () => {
  it('should create an instance', () => {
    const directive = new BackwardsDirective();
    expect(directive).toBeTruthy();
  });
});
