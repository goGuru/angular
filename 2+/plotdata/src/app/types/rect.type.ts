export class RectType{
    width:number;
    height:number;
    y:number;
    color:string;

    constructor(rectWidth:number, rectHeight:number, y:number, color:string){
      this.width = rectWidth;
      this.height = rectHeight;
      this.y = y;
      this.color = color;
    }
  }